import { IBaseHandlerConfig } from 'src/config/handler';

export type IMockHandlerConfig = IBaseHandlerConfig;

export const mockConfig: IMockHandlerConfig = {
  name: 'MockHandler',
  color: '#FFFFFF',
};
