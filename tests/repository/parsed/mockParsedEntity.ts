import { mockMessageEntity } from 'tests/repository/mockMessageEntity';

export const mockParsedEntity = {
  id: '1',
  handler: 'MockHandler',
  parsedMessage: ['this is a parsed message'],
  message: mockMessageEntity,
};
